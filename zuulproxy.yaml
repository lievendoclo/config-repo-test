zuul:
  retryable: true
  routes:
    discovery:
      path: /eureka/**
      serviceId: discovery
      retryable: true
    service-one:
      path: /service-one/**
      serviceId: service-one
      retryable: true
